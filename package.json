{
  "name": "my-portfolio",
  "version": "0.0.1",
  "scripts": {
    "start": "npm run dev",
    "build": "tsc && node dist/index.js",
    "serve": "npm run build && open dist/index.html",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
